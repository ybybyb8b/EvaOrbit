"use client";
import { useEffect,useState } from "react";
function bytes(value:string){const padding="=".repeat((4-value.length%4)%4);const raw=atob((value+padding).replace(/-/g,"+").replace(/_/g,"/"));return Uint8Array.from([...raw].map(char=>char.charCodeAt(0)));}
export function PushNotificationControl(){const[state,setState]=useState<"loading"|"unavailable"|"off"|"on">("loading");const[error,setError]=useState("");useEffect(()=>{void(async()=>{if(!("serviceWorker"in navigator)||!("PushManager"in window)||!("Notification"in window)){setState("unavailable");return;}const registration=await navigator.serviceWorker.ready;setState(await registration.pushManager.getSubscription()?"on":"off");})();},[]);async function enable(){setError("");const config=await fetch("/api/push/config").then(r=>r.json());if(!config.enabled){setError("Push is not configured on the server.");return;}if(await Notification.requestPermission()!=="granted"){setError("Notification permission was not granted.");return;}const registration=await navigator.serviceWorker.ready;const subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:bytes(config.publicKey)});const response=await fetch("/api/push/subscriptions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(subscription.toJSON())});if(response.ok)setState("on");else setError("Could not save this device.");}async function disable(){const registration=await navigator.serviceWorker.ready;const subscription=await registration.pushManager.getSubscription();if(subscription){await fetch("/api/push/subscriptions",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({endpoint:subscription.endpoint})});await subscription.unsubscribe();}setState("off");}if(state==="unavailable")return null;return <section className="push-control"><div><span className="eyebrow">NOTIFICATIONS</span><strong>{state==="on"?"This device is subscribed":"Push reminders"}</strong>{error&&<small>{error}</small>}</div>{state!=="loading"&&(state==="on"?<button onClick={()=>void disable()}>Disable</button>:<button onClick={()=>void enable()}>Enable</button>)}</section>}

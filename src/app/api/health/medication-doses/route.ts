import { NextRequest,NextResponse } from "next/server";import { apiError } from "@/lib/api";import { createMedicationDoseEvent,listMedicationDoseEvents } from "@/lib/services/period-medication";import { parseNewMedicationDoseEvent } from "@/lib/validation";
export const runtime="nodejs";
export async function GET(request:NextRequest){try{const q=request.nextUrl.searchParams,limit=Number(q.get("limit")??100),presetId=Number(q.get("medicationPresetId")),periodId=Number(q.get("periodId"));return NextResponse.json(await listMedicationDoseEvents({medicationPresetId:Number.isSafeInteger(presetId)&&presetId>0?presetId:undefined,periodId:Number.isSafeInteger(periodId)&&periodId>0?periodId:undefined,from:q.get("from")??undefined,to:q.get("to")??undefined,limit:Number.isSafeInteger(limit)?limit:100}));}catch(error){return apiError(error);}}
export async function POST(request:NextRequest){try{return NextResponse.json(await createMedicationDoseEvent(parseNewMedicationDoseEvent(await request.json())),{status:201});}catch(error){return apiError(error);}}

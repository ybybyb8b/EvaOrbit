import { NextRequest,NextResponse } from "next/server";import { apiError,parseId } from "@/lib/api";import { readRelationPhoto,saveRelationPhoto } from "@/lib/services/relation-photo";import { ValidationError } from "@/lib/validation";
export const runtime="nodejs";export const dynamic="force-dynamic";type C={params:Promise<{id:string}>};export async function GET(_:NextRequest,{params}:C){try{const photo=await readRelationPhoto(parseId((await params).id));return photo?new NextResponse(photo.bytes,{headers:{"Content-Type":photo.mime,"Content-Length":String(photo.bytes.byteLength),"Cache-Control":"private, max-age=300","X-Content-Type-Options":"nosniff"}}):new NextResponse(null,{status:404});}catch(e){return apiError(e);}}export async function POST(r:NextRequest,{params}:C){try{const file=(await r.formData()).get("file");if(!(file instanceof File))throw new ValidationError("Choose a photo");return NextResponse.json(await saveRelationPhoto(parseId((await params).id),file));}catch(e){return apiError(e);}}

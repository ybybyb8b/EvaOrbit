"use client";import Link from "next/link";import { useMemo,useState } from "react";import { FormSheet } from "@/components/form-sheet";import { Icon } from "@/components/icons";import { PageHeader } from "@/components/page-header";import type { RelationPersonSummary } from "@/lib/types";import { RelationEventEditor } from "./relation-event-editor";import { RelationPersonEditor } from "./relation-person-editor";
const amount=(n:number)=>`${n>=0?"+":"−"}¥${(Math.abs(n)/100).toFixed(2)}`;const eventLabel:Record<string,string>={expense:"Expense",gift:"Gift",repayment:"Repayment",favor:"Favor",interaction:"Interaction"};
export function RelationsView({initialPeople}:{initialPeople:RelationPersonSummary[]}){const[people,setPeople]=useState(initialPeople);const[query,setQuery]=useState("");const[panel,setPanel]=useState<"person"|"event"|null>(null);const[busy,setBusy]=useState(false);const visible=useMemo(()=>people.filter(p=>`${p.name} ${p.nickname??""} ${p.relationLabel??""}`.toLowerCase().includes(query.toLowerCase())),[people,query]);async function load(){const r=await fetch("/api/relations/people");if(r.ok)setPeople(await r.json());}const close=()=>setPanel(null);return <div className="page relations-page"><PageHeader eyebrow="LIFE" title="Relations" description="People-indexed exchanges, expenses, gifts and favors." action={<div className="page-actions"><button className="button secondary" onClick={()=>setPanel("person")}><Icon name="plus"/>Person</button><button className="button primary" onClick={()=>setPanel("event")}><Icon name="plus"/>Exchange</button></div>}/><label className="relations-search"><Icon name="search"/><input aria-label="Search people" placeholder="Find a person…" value={query} onChange={e=>setQuery(e.target.value)}/></label>{visible.length?<div className="relation-person-list">{visible.map(person=><Link href={`/relations/${person.id}`} className="relation-person-row" key={person.id}><span className="relation-avatar">{person.photoPath?<span style={{backgroundImage:`url(${person.photoPath})`}}/>:<strong>{person.name.slice(0,1)}</strong>}</span><span className="relation-person-copy"><strong>{person.name}{person.nickname&&<small> · {person.nickname}</small>}</strong><span>{person.relationLabel??"Relationship not set"}</span>{person.latestEvent?<small>{eventLabel[person.latestEvent.eventType]} · {person.latestEvent.title} · {new Date(person.latestEvent.occurredAt).toLocaleDateString()}</small>:<small>No exchanges yet</small>}</span><span className="relation-balances"><span data-zero={person.balance.settlementMinor===0}>Settlement <b>{amount(person.balance.settlementMinor)}</b></span><span data-zero={person.balance.socialMinor===0}>Social <b>{amount(person.balance.socialMinor)}</b></span></span><Icon name="arrow" variant="stroke"/></Link>)}</div>:<div className="empty-state"><span className="empty-icon"><Icon name="people"/></span><h2>{query?"No matching people":"Add the first person"}</h2><p>People stay lightweight; exchanges are the durable record.</p>{!query&&<button className="button primary" onClick={()=>setPanel("person")}>Add person</button>}</div>}{panel==="person"&&<FormSheet title="Add person" onClose={close} submitLabel="Save person" busy={busy}><RelationPersonEditor onBusy={setBusy} onSaved={()=>{close();void load();}}/></FormSheet>}{panel==="event"&&<FormSheet title="Record an exchange" onClose={close} submitLabel="Save exchange" busy={busy}><RelationEventEditor people={people} onBusy={setBusy} onSaved={()=>{close();void load();}}/></FormSheet>}</div>}

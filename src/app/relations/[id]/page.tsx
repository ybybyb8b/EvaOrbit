import type { Metadata } from "next";import { notFound } from "next/navigation";import { getRelationPersonDetail,listRelationPeople } from "@/lib/services/relations";import { RelationPersonView } from "./person-view";
export const dynamic="force-dynamic";export const metadata:Metadata={title:"Relation"};export default async function Page({params}:{params:Promise<{id:string}>}){const id=Number((await params).id),[detail,people]=await Promise.all([getRelationPersonDetail(id),listRelationPeople()]);if(!detail)notFound();return <RelationPersonView initial={detail} people={people}/>;}
